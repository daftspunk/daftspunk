october.controllers['blog/blog'] = function ($scope, $request) {

    // $scope.test = 'Not set yet';

    // $request('onTest', function(result){ $scope.test = result; })

    // $request('onTest').done(function(data){ console.log(data) })

}